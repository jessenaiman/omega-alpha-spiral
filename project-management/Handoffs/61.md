# Omega eight-floor era journey — issue #61

- Branch: `main` in `C:\SpiralDrive\omega-alpha-spiral`. PR #63 integrated the focused issue #61 journey; the older local director branch was later merged into this checkout with its commits preserved.
- Source baseline: current `origin/main` at `673aa786`, with only issue #61 runtime, tests, design assets and evidence selected from `codex/omega-floor-arc`. The older #18 worktree retains separate uncommitted work; do not edit there.
- Last result: seed 17 and 42 real-input Playwright runs passed through intro threshold and Light/Shadow/Ambition, choosing Door/Monster/Chest and crossing the selected exit; Shadow Monster recorded `fallen`, and both runs completed Floor 3. Door positions differ by floor and seed. `npm.cmd run build` and `npm.cmd run test:bot` passed (2/2 bots). The full intro-to-Floor-3 bot verified retry. [Bot report](../../artifacts/qa-61/early-floor-bot-report.json), [video](../../artifacts/qa-61/seeded-exit-bot.webm), and [capture set](../../artifacts/qa-61/capture-set.md) are current mechanical evidence; owner aesthetic approval remains open.
- Runtime target: root `index.html` and legacy `intro.html` now load the same authored intro module, which hands off through `src/intro/BootScene.ts` to `src/chapter-two/ChapterTwoScene.ts`, `WalkField.ts`, and `rooms.ts`. Root Begin and Question 1 were browser-visible on port 5212. The old `src/main.ts` core boot and separate scratch starter are outside the canonical path.
- Current intent: advance one playable journey from sparse 2.5D Atari-in-3D Light/Shadow/Ambition rooms through Floors 4–6 combat lessons, Floor 7 displaced town, and Floor 8 modern healing-core finale. Early Monster encounters can end in death and still advance. Floors 4–6 teach telegraphed enemy attacks and basic Hit/Run; both victory and death advance with distinct recorded outcomes. Floor 7 must vary architecture rather than repeat blocky structures. Two purposeful effects per floor; readable hero/path; hidden alignment and buried subtext; dialogue studio owns prose.
- Agents: early floors authored early layout and combat and are fixing Run/party-choice persistence; middle floors authored Floor 4–6 layout/art and performed static integration review; late floors authored Floor 7–8 data/art and replaced blocky town paving with three flat era districts. Lead owns `ChapterTwoScene.ts`, art direction, manifest, progress/evidence, and review.
- Design correction, 2026-09-24: the owner noted that multiple graphics eras must be visibly decided stage by stage, and the town cannot borrow eras whose source assets were never properly designed. The manifest and scorecard now propose a precise intro-to-Floor-8 technology ladder and require every misplaced Floor 7 object to cite a reviewed source-floor asset. New concept studies: artifacts/level-design/era-progression-floors-1-6-proposal.png and artifacts/level-design/floors-4-6-era-vocabulary-proposal.png. Neither is owner-approved or a playable capture. The existing town art remains an experiment. Floors 4–6 have since gained a repeated vault tile field/echo plinths, shared 16-bit shelf-front textures/banner/lamp/silhouette layers, and shared faceted vertex-color arches with a dithered low-resolution motif. Floor-6-only distance fog was added in ChapterTwoScene.ts. The existing build script passed after each source pass, but no live Floors 4–6 art review was claimed.
- New proposal: `assets/concepts/floors-1-3-source-kit-proposal.png` shows a common rail section transformed by Light, Shadow, and Ambition at one camera; its sidecar records the image prompt and review limits. This is not an approved source asset or a runtime frame. The Ambition curve must be made more square-stepped in the actual mesh.
- Next step: review the proposed era ladder and focused Floors 4–6 study with the owner. Decide the Floor 7 classic-town base, then audit one representative playable frame and reusable asset per earlier stage before scoring the town mix. After that, co-review Shadow and Ambition framing, and Floors 4–8 in live play. Existing bots prove route mechanics only; do not expand tests without need.
- Static source audit: MiddleFloorArtKit.ts now has Floor 4 repeated tile/plinth forms, Floor 5 shared sprite-front shelf textures/banner/lamp/silhouette layers, and Floor 6 faceted vertex-color arches plus a low-resolution dithered motif. Floor-6-only fog is in ChapterTwoScene.ts. LateFloorArtKit.ts has varied town forms and per-object pixel-density textures but no explicit source-floor provenance. None of these source changes proves live visual quality. The town must first have a coherent base; its misplaced era objects await approved source artifacts from Floors 1–6. These are source findings, not live visual approval.
- Check-in note: four Chapter Two unit tests still asserted the removed `continue()`/`rewriting` progression. The obsolete reboot test was removed; choice, scoring, and tie tests now cross the selected exit. `npm.cmd run test:unit` passes 22/22 and `npm.cmd run build` passes. The earlier documentation-only checkpoints used a one-commit hook bypass before this repair.
- PR path: #63 merged the focused journey into `main`. Draft #62 targets `codex/floor-one-design-integration-18` and remains an older source branch. The pre-merge integration passed `npm.cmd run build`, 22/22 unit tests, 2/2 existing real-input bots, and a root Begin/Question 1 browser check. Those checks do not establish the later local-branch merge or full Floors 4–8 play; visual approval and full-play evidence remain open.
- History: `project-management/Handoffs/18.md` and `59.md` describe previous isolated Floor One and manifest work. This handoff governs issue #61.
- Era-design update, 2026-09-24: the owner supplied `project-management/t5 Ghost terminal presentation and copy.md` as a detailed candidate reference, then clarified that **era is a configurable shader/rendering component applied per target**, not a whole-stage label or typography alone. `artifacts/level-design/omega-code-era-design.md` proposes the component controls, retained layers, and a capability ladder for four Opening passages plus Floors 1–8; `design-manifest.md` and `design-scorecard.md` link and assess it. The eight-entry source has historical caveats documented in the new reference and is not a verified stage chronology. `GhostLetters.ts` already has a lettering-specific shader seam, but props/path materials and the studio design controls are not yet generalized. Owner review of the proposed mapping and live representative frames remains open. The studio should read/edit authored `.oml`/`.omd` files; the game reads those files directly, with no studio-to-game live bridge.
