# Issue 54 — Distinctive ghost typing

- Task: [Distinctive ghost typing for Omega and Dreamweavers](https://github.com/jessenaiman/omega-alpha-spiral/issues/54). Orientation #55, decision #56.
- Workspace: `C:\Users\jesse\.codex\worktrees\b4f7\omega-alpha-spiral`
- Branch: `prototype/ghost-typing-voices`

## Text outputs

- `src/dialogue/*.omd` — one design file per voice. Colour, typing speed, typography, spatial motion.
- `src/dialogue/*.oml` — one scene file. Tag declarations plus the dialogue.
- `src/core/oml.ts` — parses both, and matches tags.
- `omega-dialogue-studio.html` — Script tab edits the `.oml` text directly; tags colour live.
- `GET /api/studio/scenes` lists scenes, `POST` writes one the game imports.
- The game imports `scene1.oml`, so an edit in the studio reaches the game.

## Status

Dialogue tooling is complete. Next work is gameplay, Blender, images, and stage design.

Not verified: the opening never reached its choice phase. That needs real input driven, not
the studio. The bot playtest has not been run.

## Skills

Work in this repo follows `.agents/skills/threejs-game-director/SKILL.md` and its siblings:
`threejs-gameplay-systems`, `threejs-aaa-graphics-builder`, `threejs-game-ui-designer`,
`threejs-debug-profiler`, `threejs-qa-release`, `threejs-3d-generator`,
`threejs-image-generator`, `threejs-audio-generator`.

Verification is what those skills specify: the canvas inspector with declared states and
`artifacts/evidence.json`. Nothing else.

## Ownership

The owner writes the dialogue. Do not restructure, reformat, or generate dialogue text.
Change the format, the parser, or the tooling when asked; leave the words alone.
