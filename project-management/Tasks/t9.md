---
tags: [omega-spiral, omega-spiral/scene-01, subsystem/release]
---
# Scene 1 · Evidence — the browser pass that proves it is real

| Field | Value |
|---|---|
| id | t9 |
| status | todo |
| owner | reviewer |
| skills | omega-spiral-environment, omega-spiral-reviewer, threejs-qa-release |
| release | Scene 1 — Ghost Terminal |
| worktree | — (evidence run against the integrated build) |
| blocked by | t5, t6, t7, t8 |
| dependencies | t5, t6, t7, t8 |
| spec | t3 |
| hermes | — not yet dispatched |
| github | none — creative ticket; the GitHub lane is technical only |
| updated | 2026-09-17 |

**Why this is a separate task:** it is the only task that may claim the scene works. It owns no source and may change none.

## Owns (file boundary)

Evidence only: `artifacts/**`, `test-results/**`, and the browser specs under `tests/browser/**`.

## Acceptance

- The scene is completed **through real keyboard input**, first to last. **Screenshots never substitute for input proof.**
- Determinism: the same seed produces the same speaker rotation and question order.
- Colour-independence: all three Dreamweavers distinguishable with colour removed.
- Reduced motion: the journey is *replaced*, not merely slowed.
- 200% zoom and keyboard focus pass.
- Canvas inspector metrics captured at the target viewports and compared against the budget table.
- `npm run test:unit`, `npm run test:browser`, and `npm run build` pass; the production build is served from static `dist/`.
- Empty browser console, empty page-error list, and empty failed-request list at the current revision.
- A scorecard pass on the ten categories with evidence lines, before and after.
- **`artifacts/game-progress.md` is maintained as the continuity file**, as the director skill requires: current intent and constraints, decisions made, completed work, pending jobs, remaining defects, and next actions. It must be readable by a fresh session after an interruption. Keep it factual and current — this is the file that survives a lost context.
- **The required browser capture set is proven through real input:** the scene completes start to finish from keyboard, and the named capture states resolve through the test hooks rather than by faking state.

## Verify

- Exact commands, with their real output recorded. A claim without a transcript is not evidence.
- Motion evidence (webm) for the journey and the pop-out.

## Output

- Not produced yet.

## Blocker

none

## Notes

- Automatic failures to check explicitly, because this scene is unusually exposed to them: the screenshot dominated by empty space the design does not justify; UI overlapping the play path; text clipping; and renderer diagnostics missing after a graphics pass.
- Report what was actually observed. If something could not be run, say so instead of substituting a plausible result.
