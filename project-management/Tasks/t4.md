---
tags: [omega-spiral, omega-spiral/scene-01, subsystem/foundation, subsystem/state]
---
# Scene 1 · Foundation — scene host, typed events, seeded randomness, pure rules

| Field | Value |
|---|---|
| id | t4 |
| status | done |
| owner | game-dev |
| skills | omega-spiral-environment, omega-spiral-game-dev, threejs-gameplay-systems |
| release | Scene 1 — Ghost Terminal |
| worktree | `.worktrees/scene-1-foundation` on branch `feat/scene-1-foundation` |
| blocked by | none — can start immediately |
| dependencies | t3 (SPEC — the authority for all rules below) |
| spec | t3 |
| hermes | `t_baf85a0e` — dispatched to `game-dev`; running |
| github | none — creative ticket; the GitHub lane is technical only |
| updated | 2026-09-17 |

**Why this is a separate task:** it is the blocking spine every other Scene 1 task sits on. It owns no visuals and no copy, so it cannot collide with the console, universe, presence, or audio tasks.

## Owns (file boundary)

`src/core/**`, `src/game/**`, `tests/unit/**`

## Acceptance

- Module boundaries exist and are respected: `core` (host, fixed-step loop, seeded RNG, input intents, typed events), `game` (affinity, turn sequence, ladder, run state), `content`, `ui`, `systems`.
- **The RED test at `tests/unit/scene-one.test.ts` passes.** That file already encodes the hidden scoring, the turn sequence, and the ladder; treat it as the contract, not as a suggestion.
- Hidden scoring awards exactly: Omega's question → the represented Dreamweaver gains 1; a Dreamweaver's own interpretation → that Dreamweaver gains 2; another Dreamweaver's option → that one gains 1.
- Turn sequence: Omega asks first and last; each Dreamweaver gets at most one turn; a choice naming an already-spoken Dreamweaver passes to the next unspoken one in canonical order.
- Ladder is strictly non-decreasing: `dot → line → box → icon`.
- One seeded RNG owns every non-authored variation. **No gameplay randomness exists outside it.**
- Same seed + same inputs reproduces the speaker rotation and the question order.
- `npm run typecheck`, `npm run test:unit`, and `npm run build` all pass.

## Verify

- `npm run test:unit` — all green, including the pre-existing `scene-one.test.ts`.
- `npm run typecheck` — clean.
- Determinism is evidenced by running the same seed twice and diffing the speaker rotation, not by assertion in prose.

## Output

**Landed.** Commit `72e976c` on `wt/t_baf85a0e`, **17 files, 2155 insertions** — `src/core/` (host, clamped fixed-step loop, one seeded RNG with forks, input intents with keyboard + gamepad parity, typed event bus, settings store, both acceptance surfaces) and `src/game/` (affinity, ladder, sequence, run state), plus the RED contract and two suites the worker added.

**Verified by the lead after the worktree freed — not taken from the handoff:**

| Check | Result |
|---|---|
| `npm run typecheck` | exit 0 |
| `npm run build` | exit 0 |
| `npm run test:unit` | **50 pass, 0 fail** |
| `tests/unit/scene-one.test.ts` | **byte-identical** to the RED contract — not weakened |

Determinism was evidenced by running the same seed twice and diffing the speaker rotation, as `## Verify` requires.

**Released:** pushed to `origin` as branch `wt/t_baf85a0e`. **Draft PR #10** open against `main`. The remote was bare before this — nothing had ever been pushed, so the work existed only on one disk until 2026-09-18.

**Check-in history:** `check-in-agent` was assigned this check-in and could not complete it — it has two confirmed faults (a dead per-profile credential, and a provider outage that killed its worker twice). The lead took over after the two permitted attempts. `check-in-agent` retains the gate.

## Blocker

none

## Notes

- Follow `test-driven-development`: the RED test already exists; observe it pass, do not delete it to make it pass.
- The three contradiction items in `t3` (question count, camera contract, the player-given name) do **not** block this task — the rules above are already resolved; only the copy they attach to is open.
- **One contradiction the worker found and refused to resolve — needs the design owner's ruling.** A scoring assertion in the RED test contradicts the `t3`/`t4` prose. The worker implemented per the test (the stated contract) and reported it rather than choosing silently. **This is still open.**
- **The gap this ticket left:** nothing wires the host into `src/main.ts`, so the `window` acceptance surfaces do not exist on a live page and the browser evidence pass (`t9`) cannot run. Follow-up filed as **GitHub issue #11** (technical lane) and dispatched on Hermes card `t_f1153b09` assigned to `omega-game-director` — boot and wiring are integration work, which the director skill keeps with the lead.
