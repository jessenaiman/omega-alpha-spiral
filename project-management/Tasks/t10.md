---
tags: [omega-spiral, omega-spiral/scene-01, subsystem/terminal, subsystem/foundation]
---
# Scene 1 · Design artifacts and copy cards — reconcile the written record

| Field | Value |
|---|---|
| id | t10 |
| status | needs-info |
| owner | omega-project-librarian |
| skills | omega-spiral-environment |
| release | Scene 1 — Ghost Terminal |
| worktree | — (documentation; no code) |
| blocked by | none |
| dependencies | t2, t3 |
| updated | 2026-09-17 |

**Why this is a separate task:** three documents now disagree about Scene 1, and the disagreement is the design owner's to settle, not an agent's. This is the task that stops the docs drifting from the build.

## Owns (file boundary)

`docs/game/design-brief.md`, `docs/game/core-loop-contract.md`, and the creative cards for the three Dreamweaver questions.

## Acceptance

- Decide the fate of `docs/game/design-brief.md` and `docs/game/core-loop-contract.md`: the design owner previously flagged them as **AI interpretations of their writing that may need deleting**. Either they are corrected to agree with `t3`, or they are marked superseded and the superseding authority named. Leaving them contradicting the build is not an option.
- The director skill requires three artifacts for a broad build — a design brief, a core-loop contract, and a level or encounter plan. For Scene 1 these are either updated to match `t3` or explicitly superseded by it. Record which.
- The **camera contract amendment** from `t6` (fixed front plane → the lemniscate journey) is written into the contract rather than left implicit.
- The three Dreamweaver **creative cards** move from provisional notes into `src/content` as copiable text, each keeping its concept and its "to improve" line.
- The four open contradictions recorded in `t3` Further Notes are each resolved or explicitly deferred with a date.

## Verify

- No repo document contradicts `t3`. A reader who follows `AGENTS.md`'s stated authority order reaches the same Scene 1 design as a reader who follows `t3`.
- The Dreamweaver wording can be changed without touching build code.

## Output

- Not produced yet.

## Blocker

This task needs the design owner's rulings. It cannot be completed by an agent, and an agent guessing at it is exactly what caused the current contradiction.

## Notes

- This is documentation work and may be delegated to `mimo` once the owner's rulings are recorded here.
- The three creative cards are already sketched in `t3` Further Notes: **Light** the warner who tells you not to; **Shadow** the inviter who deals in wishes and takes something back; **Ambition** the one that asks what it is all worth. All three are aspects of one persona — their combined dialogue should complete a whole, and no single one should read as the complete voice.
